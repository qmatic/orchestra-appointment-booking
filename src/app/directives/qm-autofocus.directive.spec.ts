import { QmAutofocusDirective } from './qm-autofocus.directive';

describe('QmAutofocusDirective', () => {
  it('should create an instance', () => {
    const directive = new QmAutofocusDirective();
    expect(directive).toBeTruthy();
  });
});
