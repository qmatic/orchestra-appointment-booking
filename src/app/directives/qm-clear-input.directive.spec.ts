import { QmClearInputDirective } from './qm-clear-input.directive';

describe('QmClearInputDirective', () => {
  it('should create an instance', () => {
    const directive = new QmClearInputDirective();
    expect(directive).toBeTruthy();
  });
});
