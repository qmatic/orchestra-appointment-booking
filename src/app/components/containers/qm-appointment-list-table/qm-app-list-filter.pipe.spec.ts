import { QmAppListFilterPipe } from './qm-app-list-filter.pipe';

describe('QmAppListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new QmAppListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
